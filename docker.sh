docker logs container_id_or_name
docker images
docker ps
docker run -d -p 8000:8000 my-django-app
docker --version

<h1>Good Blog to Learn Both Django Docker and </h1>


docker compose build
docker stop $(docker ps -a -q)
docker compose up
